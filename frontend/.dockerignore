# Node modules directory (dependencies will be installed inside the container)
node_modules

# Next.js build output (can be built inside the container or ignored for development builds)
.next

# Static files output (if generated via `next export`)
out

# Dockerfile and Docker ignore file itself (we don't want them copied into the image)
Dockerfile
.dockerignore

# Git files and folders
.git
.gitignore

# Local development environment configuration files
*.env.local
*.env.*.local

# Logs and temporary files
npm-debug.log
yarn-debug.log
yarn-error.log

# VS Code workspace settings
.vscode/

# MacOS-specific files
.DS_Store

# Windows-specific files
Thumbs.db

# IDE and editor files
.idea/
*.swp
